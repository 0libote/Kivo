# Generated Astryx theme artefacts (src/theme/built) are compiled output:
# their repeated token and component-override blocks are inherent to the
# generator, so exclude them from copy-paste detection.
sonar.cpd.exclusions=src/theme/built/**
